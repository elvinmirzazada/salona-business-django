// Login page functionality - Updated for Django form submission
document.addEventListener('DOMContentLoaded', function() {
    // Check if user is already authenticated and redirect if needed
    // This is now handled server-side in Django LoginView.get()

    // No need for JavaScript form submission - Django handles it
    // Keep this file minimal for any future client-side enhancements

    console.log('Login page loaded - using Django form submission');
});
